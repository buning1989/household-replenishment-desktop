# Vercel 服务端环境变量（比赛 Web Demo）
# 只写变量名，不写真实值。在 Vercel 项目设置中配置。

# DashScope API Key（必填）
DASHSCOPE_API_KEY=

# 对话模型（可选，默认 qwen-plus）
DASHSCOPE_CHAT_MODEL=

# 订单识别模型（可选，默认 qwen3-vl-plus）
DASHSCOPE_OCR_MODEL=
